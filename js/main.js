// ======================================================
// js/main.js
// 앱 실행 진입점
//
// 담당
// - app.js의 초기화 실행
//
// 원칙
// - 앱 초기화 로직은 app.js에서 담당한다.
// - main.js에서는 초기화 로직을 중복하지 않는다.
// - localStorage에 직접 접근하지 않는다.
// - 화면 렌더링 및 사용자 동작은 각 담당 파일에서 처리한다.
// ======================================================


document.addEventListener(
  "DOMContentLoaded",
  () => {

    // 앱 초기화
    //
    // app.js에서 실제 초기화 순서를 관리한다.
    initializeApp();
  }
);